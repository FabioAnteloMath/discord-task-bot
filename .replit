# Arquivo de configuração do Replit
# Diz ao Replit qual comando usar para iniciar o projeto

run = "python bot.py"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "python bot.py"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8080
externalPort = 80
